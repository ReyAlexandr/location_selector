## 0.0.3

* Await asynchronous `onLocationSelected` callbacks so the selector remains
  busy while consumers process a selection.
* Upgrade `geolocator` to `^14.0.2`.
* Add widget tests for asynchronous selection completion and failure.

## 0.0.2

* Update package repository metadata.

## 0.0.1

* Initial release with debounced search, suggestion resolution, and GPS
  selection support.
